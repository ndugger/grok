# pragma once

# include <string>
# include <vector>

namespace grok::commands {

    using namespace std;

    int update (string command_from, vector<string> command_arguments, bool triggered_by_user = true) {
        return 0;
    }
}