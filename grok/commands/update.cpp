# pragma once

# include <string>
# include <vector>

namespace grok::commands {

    using namespace std;

    int update (const string& command_origin, const bool& command_by_user, const vector<string>& command_arguments) {
        return 0;
    }
}