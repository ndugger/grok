# ifndef COMMANDS_HELP
# define COMMANDS_HELP

# include <queue>
# include <string>

using namespace std;

namespace grok::commands {
    int help_command (queue<string> ar) {
        return 0;
    }
}

# endif